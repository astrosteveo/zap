# Zap Configuration (~/.zaprc)
#
# Optional file for customizing Zap behavior.
# Most users don't need this - the defaults are sensible.

# Prompt - disable if using Starship, Powerlevel10k, etc.
# zstyle ':zap:prompt' enable 'no'

# History
# zstyle ':zap:history' size '100000'
# zstyle ':zap:history' share 'yes'    # Share across sessions

# Completion
# zstyle ':zap:completion' case-sensitive 'yes'

# Terminal titles - disable if your prompt handles this
# zstyle ':zap:terminal' title 'no'
